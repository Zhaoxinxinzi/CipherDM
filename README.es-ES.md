

# CipherDM
El primer 3PC framework para el muestreo de Modelos de Difusión.

## Ejecutar

```console
docker build -t myubuntu:v1 --build-arg http_proxy=## --build-arg https_proxy=## --network host .
docker run --rm --network=host myubuntu:v1
```
